from std.async.thread import Thread

def worker(n: int) -> int:
    return n * 2

def main():
    mut pool = ThreadPool.auto()
    pool.spawn(worker, 5)
    pool.wait()
    pool.free()
    print("done")
